from setuptools import setup, find_packages

setup(
    name="Comparative-study-of-cnn-based-architectures-on-subpipe",
    version="0.1.0",
    package_dir={"": "src"},
    packages=find_packages(where="src"),
)
